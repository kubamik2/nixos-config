inputs: {
  programs.alacritty = {
    enable = true;
    settings = {
      # window = {
      #   opacity = 0.9;
      #   blur = true;
      # };

      # font = {
        # normal = { family = "JetBrainsMono Nerd Font"; style = "Regular"; };
      # };
    };
  };
}
